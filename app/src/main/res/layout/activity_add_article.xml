<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#ebe9e4">

    <TextView
        android:id="@+id/BlogTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:text="SimpleBlog"
        android:textColor="#000000"
        android:textSize="60sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/addArticleTitleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/BlogTitle"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:text="Ajouter un article"
        android:textColor="#000000"
        android:textSize="30sp"
        android:textStyle="bold" />

    <LinearLayout
        android:id="@+id/articleFormLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/addArticleTitleTextView"
        android:layout_margin="16dp"
        android:orientation="vertical"
        android:padding="16dp">

        <EditText
            android:id="@+id/articleTitleEditText"
            android:layout_width="match_parent"
            android:layout_height="65dp"
            android:paddingLeft="10dp"
            android:background="@drawable/rounded_border"
            android:hint="Titre"
            android:textColor="#000000"
            android:textSize="25sp" />

        <EditText
            android:id="@+id/articleContentEditText"
            android:layout_width="match_parent"
            android:layout_height="300dp"
            android:layout_marginTop="16dp"
            android:background="@drawable/rounded_border"
            android:hint="Contenu"
            android:textColor="#000000"
            android:gravity="top|left"
            android:paddingLeft="10dp"
            android:textSize="25sp" />

    </LinearLayout>

    <Button
        android:id="@+id/addArticleButton"
        android:layout_width="325dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/articleFormLayout"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:background="@drawable/rounded_border_button"
        android:text="Ajouter"
        android:textColor="#FFFFFF"
        android:textSize="25sp"
        android:textStyle="bold" />

    <ImageButton
        android:id="@+id/closeButton"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_margin="16dp"
        android:background="@drawable/rounded_circle"
        android:src="@drawable/close" />


</RelativeLayout>
